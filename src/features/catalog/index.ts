/**
 * Public API of the `catalog` feature.
 *
 * Cross-feature imports go through this barrel only — never reach into
 * `features/catalog/screens/…` from another slice.
 */

export * from './screens';
