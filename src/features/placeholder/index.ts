/**
 * Public API of the `placeholder` feature.
 *
 * Cross-feature imports go through this barrel only — never reach into
 * `features/placeholder/screens/…` from another slice.
 */

export * from './screens';
