/**
 * Public API of the `finance` feature.
 *
 * Cross-feature imports go through this barrel only — never reach into
 * `features/finance/screens/…` from another slice.
 */

export * from './screens';
