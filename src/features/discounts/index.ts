/**
 * Public API of the `discounts` feature — branch discount claims.
 *
 * Not the per-line discount on a sale, and not a stock return. See
 * `DiscountsScreen` for the three-way distinction.
 */
export { DiscountsScreen } from './screens/DiscountsScreen';
