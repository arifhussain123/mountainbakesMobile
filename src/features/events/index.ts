/**
 * Public API of the `events` feature.
 *
 * Cross-feature imports go through this barrel only — never reach into
 * `features/events/screens/…` from another slice.
 */

export * from './screens';
