/**
 * Public API of the `appearance` feature.
 *
 * Cross-feature imports go through this barrel only — never reach into
 * `features/appearance/screens/…` from another slice.
 */

export * from './screens';
export * from './components';
