/**
 * Public API of the `support` feature.
 *
 * Cross-feature imports go through this barrel only — never reach into
 * `features/support/screens/…` from another slice.
 */

export * from './screens';
